model tLike {
    id Int @id @default(autoincrement())
    user_id String
    user mUser @relation(fields: [user_id], references: [id])
    post_id Int
    post mPost @relation(fields : [post_id],references: [id])
    

    @@unique([user_id,post_id])



}